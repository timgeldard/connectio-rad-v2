/** Caveats that keep the standalone Lab Board truthful while it is POC-backed. */
export const LAB_BOARD_STANDALONE_CAVEATS: readonly string[] = [
  'Static Claude Design export data - not a governed SAP QM live feed.',
  'Failure and warning cards are visual POC examples until connected to app-facing contracts.',
  'Do not treat this wallboard as production release, reject, or hold decision evidence.',
]
