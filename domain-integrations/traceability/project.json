{
  "name": "di-traceability",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "domain-integrations/traceability/src",
  "projectType": "library",
  "tags": ["scope:traceability", "layer:integration", "type:lib"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p tsconfig.json",
        "cwd": "domain-integrations/traceability"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["domain-integrations/traceability/**/*.{ts,tsx}"]
      }
    }
  }
}
